 <%= form_for @campaign, url: user_campaigns_path(@user, @campaign) do |f| %>    
    <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
        <%= f.label :due_date %>
        <%= f.datetime_local_field :due_date, class: "form-control" %>
    </div>     
    <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, class: "form-control" %>
    </div>    
    <%= f.submit t('Save', :default => "Save"), class: "btn btn-primary" %>
<% end %>